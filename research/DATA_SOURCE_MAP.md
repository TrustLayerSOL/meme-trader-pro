# MemeTraderPro Data Source Map

Last updated: 2026-05-16

This map explains where the dashboard and bot state comes from. Use it before changing a panel, parser, storage schema, or strategy rule.

## Primary State Files

| Source | Purpose | Main Readers | Main Writers |
| --- | --- | --- | --- |
| `live_state.json` | Root live signal snapshot used by the dashboard. | `dashboard/dashboard.py`, analyzers | scanner / bot runtime |
| `data/live_state.json` | Data-folder live state snapshot. | storage sync, health checks | runtime utilities |
| `data/paper_trades.json` | Open, closed, and failed paper trades, including `paper_lane=main` or `paper_lane=exploration` when known. | dashboard, performance analyzer, postmortem, replay, native Portfolio/PnL view, native Paper Profitability Review | `paper_trader.py`, bot runtime |
| `data/postmortems/paper_closed.jsonl` (optional) | Append-only structured Phase A trade post-mortems (classifications + tracked BUILD_PLAN fields). Generated by `python -m analysis.run_postmortem_batch`; not written by runtime yet. | local analysis/review | batch CLI (`analysis/trade_postmortem.py`) |
| `data/rejected_signals/rejections.jsonl` (optional) | Append-only no-trade rows when `analysis_rejection_log_enabled` is true (default on). Rows now carry V2 signal context where available: triggering wallets, cluster timing, market/liquidity/token age, holder/risk context, execution assumptions, scoring tail, and market-regime tags. See **Rejection log semantics** below for `source` tags and how the human-readable rejection line is chosen. Batch backfill: `python -m analysis.export_sqlite_skips` (`source=sqlite_skip_export`, idempotent by `decision_id`). Counterfactual fields are placeholders until replay pricing exists. | filter calibration, replay visibility | `analysis.rejection_hooks`, SQLite backfill, `core.signal_context` |
| `data/signal_contexts/contexts.jsonl` (optional) | Append-only V2 signal contexts captured from runtime no-trade paths. Runtime-generated and ignored by git. Current wiring covers scanner and Market Radar rejected signals; accepted paper-entry wiring is next. | future replay and wallet signal analysis | `analysis.signal_context_logger`, `core.signal_context`, `analysis.rejection_hooks` |
| `data/replay_visibility_report.json` | Generated review report from rejected/no-trade rows. Exposes triggering wallets, wallet quality, cluster composition, liquidity state, execution assumptions, market regime, and replay notes. Review-only; not a backtest engine. | operator analysis, future replay lab | `utils/build_replay_visibility_report.py`, `core.replay_visibility` |
| `data/historical_replay/replay_events.jsonl` | Generated review-only historical replay event dataset. Normalizes accepted trades, failed trades, rejected signals, and wallet observations into one replay event schema while keeping later outcome labels outside decision context. Each event declares fixed `30s`, `2m`, `5m`, and `15m` evaluation windows plus latency/slippage/liquidity/failed-fill assumptions. Later outcome labels can include per-window `windows` entries. Ignored by git as local generated research data. | historical replay analysis, leakage checks, wallet feedback-loop validation | `utils/build_historical_replay_dataset.py`, `research.historical_replay_dataset`, `utils.build_wallet_outcome_ledger`, `research.outcome_linker` |
| `data/historical_replay/summary.json` | Compact summary for the generated historical replay dataset: event counts, unsafe/leakage count, fill-status counts, evaluation windows, per-window outcome counts, mode, schema, and output path. Ignored by git as local generated research data. | operator/research review | `utils/build_historical_replay_dataset.py` |
| `ROADMAP.md` | Root research roadmap for Quant Wallet Tracker V2. Defines active priorities and frozen lanes. | agents, operator review | lead agent |
| `RESEARCH_RULES.md` | Governance rules for signal/filter/model/replay promotion. Keeps live execution frozen and requires evidence before logic promotion. | agents, operator review | lead agent |
| `SIGNAL_REGISTRY.md` | Registry of tracked signals, definitions, source paths, timestamp availability, rationale, decision-time safety, and validation status. | agents, future experiments | lead agent |
| `EXPERIMENT_LOG.md` | Required experiment log with hypothesis, files changed, data used, sample size, baseline, new result, conclusion, and next action. | agents, operator review | lead agent |
| `data/manual_watchlist.json` | Manually protected mints, watchdog state, and protected-position amount metadata. Operator-owned fields are preserved during watchdog updates; `auto_sell` remains locked off. | dashboard, desktop GUI, watchdog, SQLite sync | dashboard, desktop protected-token add form, desktop protected amount editor, `core/rug_watchdog.py` |
| `data/wallet_performance.json` | Wallet stats, paper-copy evidence, and wallet scores. | dashboard, wallet labeler, copy engine | wallet performance backfill/runtime |
| `data/wallet_behavior.json` | Rolling 7d/30d wallet behavior report, behavior labels, and per-wallet postmortem rollups with best/worst paper outcomes, exit reasons, failure reasons, and average hold time. Advisory only. | native Wallets tab, wallet lifecycle review | `core/wallet_behavior.py`, `core/wallet_discovery_scheduler.py` |
| `data/candidate_wallets.json` | Watch-only candidate wallets discovered from local scanner history and optional read-only early-buyer chain evidence. This file is review-only and must not execute trades or auto-promote wallets. Candidate discovery reads `data/bad_wallets.json`, excludes those wallets from active candidate ranking, and records them separately under `blocked_candidates` for audit visibility. | future native Wallets review panel, lead-agent review | `utils/discover_candidate_wallets.py` |
| `data/paper_watch_wallets.json` | Lower-trust wallet lane created from reviewed candidates. Runtime can observe these wallets and collect paper outcomes, but they are not live-trade drivers and do not mutate `data/tracked_wallets.json`. Approved weak wallets can be retained as evidence rows with `status=demote_review`; the bot loader excludes those from paper-watch observation. Sync now uses `data/bad_wallets.json` as a re-entry guard and marks blocked rows with `blocked_reason=bad_wallet_list`. | bot startup, scanner, wallet lifecycle review, wallet apply utility | `utils/sync_paper_watch_wallets.py`, `core/wallet_lifecycle.py`, `utils/apply_wallet_review.py` |
| `data/wallet_discovery_status.json` | Latest wallet discovery scheduler cycle summary, including candidate count, blocked bad-wallet count, paper-watch count, and dry-run apply preview. Does not contain raw tracked wallet lists. | Ops/System freshness, operator review | `core/wallet_discovery_scheduler.py`, `utils/run_wallet_discovery_scheduler.py` |
| `data/wallet_review_decisions.json` | Operator approvals for wallet promotions/demotions. Only approved decisions are eligible for the apply tool. Replay-based review recommendations can be synced here, but wallet-list apply still requires candidate-audit evidence and paper-watch/tracked-list gates. | wallet apply utility, native Wallets review actions | operator/manual edit, `POST /api/wallet-review-decision`, `utils/sync_replay_wallet_decisions.py` |
| `data/wallet_list_update_audit.json` | Recent wallet-list apply audit trail with changes, skipped decisions, summary, and backup directory. | operator review, future Ops/System panel | `utils/apply_wallet_review.py` |
| `data/wallet_quant_report.json` | Quant Wallet Tracker report with tier, metrics, behavior profile, relationship summary, review-only behavior score, evidence, recommendation, and reason rows. Includes explicit unknowns instead of guessed fields. | wallet quant endpoint/UI, operator review | `utils/build_wallet_quant_report.py`, `core.wallet_quant`, `wallets/*` |
| `data/wallet_outcome_ledger.json` | Review-only wallet outcome ledger generated from unified accepted-trade records, rejected-signal records, and wallet-performance signal observations. Aggregates total signals, accepted/rejected counts, known outcomes, runner/rug/dead participation, average PnL after signal, liquidity, token age, cluster duration, market-regime breakdown, promotion/demotion score, confidence, and recommendation. Later outcomes are labeled by `research.outcome_labeler`; snapshot-linked outcomes are created by `research.outcome_linker`; review recommendations are owned by `wallets.wallet_promotion_engine`. Wallet-performance signal observations preserve decision-time context, enrich missing market context only from prior token snapshots, and store later snapshot results only in `later_token_outcome`. | future wallet promotion/demotion review | `utils/build_wallet_outcome_ledger.py`, `wallets.wallet_outcome_ledger`, `wallets.wallet_promotion_engine`, `research.signal_schema`, `research.outcome_labeler`, `research.outcome_linker` |
| `data/wallet_baseline_comparison.json` | Review-only comparison of the older wallet quant report against the newer wallet outcome ledger. Flags agreement, conflict, unconfirmed quant signals, quant-only wallets, and ledger-only wallets. | operator/research review | `utils/build_wallet_baseline_comparison.py`, `wallets.wallet_baseline_comparison` |
| `data/wallet_candidate_audit.json` | Review-only audit report for snapshot-linked, replay-review, and Stage 4 promotion/demotion candidates. Summarizes sample gates, known/fillable outcomes, runner/rug/dead counts, source coverage, recommendation evidence, replay or Stage 4 source metrics, and baseline comparison status. Explicitly blocks direct auto-apply; the wallet apply utility consumes this report only as evidence for approved decisions. Exposed read-only through `/api/wallet-candidate-audit` and Obsidian `WalletCandidateReviews`. | operator/research review, wallet apply utility, API/Obsidian review | `utils/build_wallet_candidate_audit.py`, `wallets.wallet_candidate_audit`, `desktop_api.py`, `obsidian_export/candidate_note.py` |
| `data/reports/wallet_reviews/wallet_candidate_decision_prep.json` | Generated draft-only decision-prep report built from `data/wallet_candidate_audit.json`. It can prepare proposed `approve_promotion` or `approve_demotion` records for current human-review candidates, but proposed records remain `approved=false` and cannot mutate wallet lists. Ignored by git as local generated research data. | operator review before recording explicit wallet approvals | `utils/build_wallet_candidate_decision_prep.py`, `wallets.wallet_candidate_decision_prep`, `/api/wallet-candidate-decision-prep` |
| `data/reports/wallet_reviews/wallet_candidate_review_summary.json` | Generated review-only summary of the current wallet candidate audit queue. Separates actionable review rows, risk-review-required rows, insufficient-evidence rows, and resolved/applied rows. Ignored by git as local generated research data. | operator/research review before collecting more wallet evidence or recording approvals | `utils/build_wallet_candidate_review_summary.py`, `wallets.wallet_candidate_review_summary`, `/api/wallet-candidate-review-summary` |
| `data/reports/wallet_reviews/wallet_candidate_collection_plan.json` | Generated review-only evidence collection plan for the current Stage 4 audit queue. Converts insufficient-evidence and risk-review rows into concrete collection steps: collect outcomes plus market context, collect outcome labels, or manual risk review. Ignored by git as local generated research data. | operator/research evidence collection planning | `utils/build_wallet_candidate_collection_plan.py`, `wallets.wallet_candidate_collection_plan`, `/api/wallet-candidate-collection-plan` |
| `data/reports/wallet_reviews/wallet_candidate_collection_batch_report.json` | Generated review-only Stage 4 evidence-chain batch report. Runs the allowed report-refresh sequence from evidence enrichment through candidate collection planning, blocks wallet-list apply/trading commands before execution, and records per-step pass/fail output. Ignored by git as local generated research data. | operator/research verification before acting on Stage 4 review rows | `utils/run_wallet_candidate_collection_batch.py`, `/api/wallet-candidate-collection-batch` |
| `data/reports/wallet_reviews/wallet_candidate_blocker_reducer.json` | Generated review-only Stage 4 blocker reducer. Combines candidate audit, collection plan, and latest batch report into current blocker counts, missing-evidence totals, top blocked wallets, and next actions. Ignored by git as local generated research data. | operator/research evidence prioritization before promotion/demotion review | `utils/build_wallet_candidate_blocker_reducer.py`, `wallets.wallet_candidate_blocker_reducer`, `/api/wallet-candidate-blockers` |
| `data/reports/wallet_reviews/wallet_candidate_context_recovery_queue.json` | Generated review-only queue for wallets blocked on both outcome labels and decision-time market context. Ranks recovery targets and lists suggested existing evidence sources without fetching or fabricating data. Ignored by git as local generated research data. | operator/research targeted evidence recovery planning | `utils/build_wallet_candidate_context_recovery_queue.py`, `wallets.wallet_candidate_context_recovery_queue`, `/api/wallet-candidate-context-recovery` |
| `data/reports/wallet_reviews/wallet_candidate_context_recovery_runner.json` | Generated bounded read-only Stage 4 recovery report. Links context-recovery target wallets to existing enriched wallet evidence, missing-market-context rows, replay events, and historical backfill records. It preserves remaining blockers and does not fetch external data, fabricate context, promote/demote wallets, or mutate lists. Ignored by git as local generated research data. | operator/research evidence recovery audit before trust changes | `utils/build_wallet_candidate_context_recovery_runner.py`, `wallets.wallet_candidate_context_recovery_runner`, `/api/wallet-candidate-context-recovery-runner` |
| `data/reports/wallet_reviews/wallet_candidate_context_recovery_closeout.json` | Generated read-only Stage 4 closeout report. Converts blocked recovery-runner wallets into exact next actions: backfill market context plus outcome labels, backfill outcome labels only, link transaction artifacts, manual risk review, or ready for candidate review. It never approves, promotes, demotes, trades, fetches external data, or mutates wallet lists. Ignored by git as local generated research data. | operator/research next-action selection for blocked Stage 4 wallets | `utils/build_wallet_candidate_context_recovery_closeout.py`, `wallets.wallet_candidate_context_recovery_closeout`, `/api/wallet-candidate-context-recovery-closeout` |
| `data/wallet_replay_scorecard.json` | Review-only wallet scorecard generated from historical replay events. Aggregates fixed-window outcome coverage/performance, fillability, market-regime exposure, co-entry partners, and repeated co-entry pairs. Generated local research data ignored by git. | wallet ecosystem review, replay-quality review | `utils/build_wallet_replay_scorecard.py`, `wallets.wallet_replay_scorecard`, `data/historical_replay/replay_events.jsonl` |
| `data/wallet_cycle_report.json` | Generated review-only wallet feedback-loop summary. Combines tracked wallets, paper-watch rows, blocked/demoted wallets, bad wallets, candidate-audit queue counts, replay scorecard coverage, approved review decisions, latest apply audit, and attention flags. Ignored by git as local generated research data. | operator/research review, future Obsidian/API export | `utils/build_wallet_cycle_report.py`, `wallets.wallet_cycle_report` |
| `data/wallet_candidate_quality_report.json` | Generated review-only ranking of active candidate wallets by current evidence quality. Excludes bad-listed wallets from ranked candidates, keeps blocked candidates visible, and scores recency, runner overlap, early-entry evidence, sell-heavy risk, behavior labels, and review-policy hints. Ignored by git as local generated research data. | operator/research review, Obsidian/API export | `utils/build_wallet_candidate_quality_report.py`, `wallets.wallet_candidate_quality` |
| `data/wallet_candidate_quality_review.json` | Generated review-only cross-check of top candidate-quality wallets against replay and outcome-ledger evidence. Classifies rows as promotion-review-ready, observe-more, risk-review, or reject-review; never applies wallet-list changes. Ignored by git as local generated research data. | operator/research review, Obsidian/API export | `utils/build_wallet_candidate_quality_review.py`, `wallets.wallet_candidate_quality_review` |
| `data/reports/wallet_backfills/wallet_stage4_review_report.json` | Generated review-only Stage 4 promotion/demotion gate. Consumes the integrated Stage 3 wallet evidence scorecard and classifies wallets as promotion-review-ready, hold-more-data, risk-review-required, or demotion/block-review. It never auto-applies wallet-list changes, promotes, demotes, or trades. Ignored by git as local generated research data. | Stage 4 human-review gate, future wallet apply workflow input | `utils/build_wallet_stage4_review.py`, `wallets.wallet_stage4_review` |
| `data/wallet_candidate_evidence_plan.json` | Generated review-only collection plan for the top candidate-quality shortlist. Shows missing replay-known, fillable, and outcome-label counts for each candidate wallet, plus next collection action. Ignored by git as local generated research data. | operator/research review, Obsidian/API export | `utils/build_wallet_candidate_evidence_plan.py`, `wallets.wallet_candidate_evidence_plan` |
| `data/wallet_candidate_backfill_targets.json` | Generated review-only backfill target list for the candidate evidence plan. Groups local historical replay events by wallet and classifies the next action as collect wallet history, backfill outcome labels, collect more replay events, resolve risk flags, or hold for review. Ignored by git as local generated research data. | operator/research review, Obsidian/API export | `utils/build_wallet_candidate_backfill_targets.py`, `wallets.wallet_candidate_backfill_targets` |
| `data/wallet_backfills/wallet_history_backfill_report.json` | Generated review-only wallet-history backfill report. Summarizes processed target wallets, partial/full/blocked status, evidence row counts, preserved raw transaction path, and preliminary wallet-quality metrics. Ignored by git as local generated research data. | operator/research review, Obsidian/API export | `utils/run_wallet_history_backfill.py`, `wallets.wallet_history_backfill` |
| `data/wallet_evidence/wallet_history_evidence.jsonl` | Append-only generated wallet evidence rows from read-only wallet-history backfills. Rows include wallet, token mint, action, timestamp, signature, outcome windows when known, missing fields, confidence score, and parser version. Ignored by git as local generated research data. | wallet evidence review, future replay/outcome linking | `utils/run_wallet_history_backfill.py`, `wallets.wallet_evidence_models`, `wallets.wallet_history_parser` |
| `data/wallet_backfills/wallet_evidence_enrichment_report.json` | Generated review-only enrichment summary for wallet-history evidence. Counts enriched rows, rows with entry context, rows with known outcome labels, missing market context, and missing outcome labels. Ignored by git as local generated research data. | operator/research review, Obsidian/API export | `utils/enrich_wallet_history_evidence.py`, `wallets.wallet_evidence_enrichment` |
| `data/wallet_evidence/wallet_history_evidence_enriched.jsonl` | Generated enriched wallet evidence rows. Decision-time fields use only prior market snapshots; later outcome labels stay separate for evaluation. Ignored by git as local generated research data. | wallet evidence review, future promotion/demotion evidence gates | `utils/enrich_wallet_history_evidence.py`, `wallets.wallet_evidence_enrichment` |
| `data/wallet_backfills/wallet_missing_market_context_report.json` | Generated review-only queue for wallet evidence rows that still lack decision-time market context. Groups rows by mint, dedupes repeated evidence, excludes quote mints, and declares future snapshot/tick backfill windows. Ignored by git as local generated research data. | operator/research review, Obsidian/API export, future market-context backfill | `utils/build_wallet_missing_market_context_targets.py`, `wallets.wallet_missing_market_context` |
| `data/reports/wallet_backfills/wallet_evidence_readiness_report.json` | Generated review-only Stage 3 wallet evidence readiness report. Cross-checks candidate backfill targets, wallet-history collection, evidence dedupe state, enrichment coverage, missing-market-context targets, and trusted market-context readiness. It can mark the evidence collection contract complete while still reporting `0%` wallet score readiness when outcomes or market context are insufficient. Ignored by git as local generated research data. | Stage 3 completion gate, wallet evidence quality review | `utils/build_wallet_evidence_readiness.py`, `research.wallet_evidence_readiness` |
| `data/reports/wallet_backfills/wallet_evidence_recommendations_report.json` | Generated review-only Stage 3 wallet evidence recommendation report. Labels candidate wallets as `paper_watch_candidate`, `observe_more`, `risk_review`, or `hold_no_edge`, and adds `do_not_promote_yet` while wallet score readiness is insufficient. It never auto-applies wallet-list changes, promotes wallets, or trades. Ignored by git as local generated research data. | Stage 3 wallet review buckets, operator review, future Obsidian/API export | `utils/build_wallet_evidence_recommendations.py`, `wallets.wallet_evidence_recommendations` |
| `data/reports/wallet_backfills/wallet_evidence_lifecycle_report.json` | Generated review-only Stage 3 wallet evidence lifecycle report. Groups wallet-history evidence by wallet and token mint to identify observed round trips, buy-only/open positions or unseen exits, sell-only/missing entries, hold durations, action sequences, and net token accumulation/distribution. It does not infer PnL, price, liquidity, promotion, demotion, or execution. Ignored by git as local generated research data. | Stage 3 behavior review, wallet lifecycle evidence, future Obsidian/API export | `utils/build_wallet_evidence_lifecycle.py`, `wallets.wallet_evidence_lifecycle` |
| `data/reports/wallet_backfills/wallet_evidence_scorecard_report.json` | Generated review-only Stage 3 completion scorecard. Combines readiness, recommendation buckets, lifecycle behavior, and enrichment coverage into one per-wallet review report with blockers and next actions. It marks Stage 3 engine completion separately from wallet score readiness and never promotes, demotes, or trades. Ignored by git as local generated research data. | Stage 3 completion artifact, Stage 4 review input, future Obsidian/API export | `utils/build_wallet_evidence_scorecard.py`, `wallets.wallet_evidence_scorecard` |
| `data/reports/wallet_backfills/evidence_layer_completion_report.json` | Generated review-only Evidence Layer closeout. Confirms evidence rows are present, deduped, classified, and routed while keeping wallet score readiness, outcome-label gaps, market-context gaps, and trusted-promotion blockers explicit. It never promotes, demotes, mutates wallet lists, trades, or treats incomplete evidence as edge. Ignored by git as local generated research data. | Evidence Layer completion gate, operator/research handoff to replayable token timelines | `utils/build_evidence_layer_completion.py`, `research.evidence_layer_completion`, `/api/evidence-layer-completion` |
| `data/wallet_backfills/market_context_recovery_report.json` | Generated review-only migration recovery report. Summarizes decision-time market snapshots recovered from surviving JSON/JSONL artifacts after local SQLite loss. It inserts only recovered `token_snapshots`; it does not fabricate missing prices, create swap ticks, promote wallets, or alter trading logic. Ignored by git as local generated research data. | migration recovery audit, wallet evidence enrichment, outcome ledger rebuild | `utils/recover_market_context_from_json.py` |
| `data/wallet_backfills/raw_transactions/*.jsonl` | Versioned raw transaction preservation from wallet-history backfill runs. Used for auditability and parser replay. Ignored by git as local generated research data. | parser audit, future replay-safe enrichment | `utils/run_wallet_history_backfill.py` |
| `data/reports/historical_backfill/historical_market_context_backfill_report.json` | Generated review-only historical market-context backfill report. Classifies missing wallet evidence against preserved raw transactions, recovers only transaction-derived quote-per-token context when a target token delta and quote delta are both present, and records explicit block reasons for missing transaction, missing price, missing liquidity, missing market cap, or insufficient data. Ignored by git as local generated research data. | historical context audit, wallet evidence triage | `utils/backfill_historical_market_context.py`, `wallets.historical_market_context_backfill` |
| `data/reports/historical_backfill/missing_raw_transaction_recovery_report.json` | Generated review-only recovery report for exact transaction signatures missing from the historical market-context backfill. Fetches only read-only `getTransaction` records, preserves raw transactions under `data/wallet_backfills/raw_transactions/`, and does not parse prices, alter wallet lists, or touch execution. Ignored by git as local generated research data. | historical context audit, raw artifact recovery | `utils/recover_missing_raw_transactions.py`, `wallets.missing_raw_transaction_recovery` |
| `data/reports/historical_backfill/onchain_market_context_recovery_report.json` | Generated review-only home-built recovery report for historical liquidity and market-cap context. Reads local raw transaction token balances, identifies non-wallet pool/vault owners with target-token plus quote-token reserves, estimates two-sided liquidity from quote reserves, and blocks market cap until decision-time token supply exists. Provider snapshots are validation/fallback only. Ignored by git as local generated research data. | historical context audit, replay realism, wallet evidence triage | `utils/recover_onchain_market_context.py`, `wallets.onchain_market_context_recovery` |
| `data/reports/historical_backfill/onchain_supply_evidence_report.json` | Generated review-only supply evidence report. Reads on-chain market-context records and raw transaction token balances, recovers token decimals when available, accepts only decision-time supply fields as supply evidence, and blocks current-only or missing supply from wallet scoring. Ignored by git as local generated research data. | historical context audit, market-cap readiness, replay realism | `utils/build_onchain_supply_evidence.py`, `wallets.onchain_supply_evidence` |
| `data/reports/historical_backfill/replay_realism_readiness_report.json` | Generated review-only Stage 6 readiness report. Combines the historical replay summary, trusted market-context gate, and supply-evidence gate to prove whether the replay realism contract is complete and whether historical market context is score-ready. It can mark the Stage 6 realism contract complete while still blocking incomplete data from wallet scoring. Ignored by git as local generated research data. | replay realism completion gate, Stage 8 data-coverage queue | `utils/build_replay_realism_readiness.py`, `research.replay_realism_readiness` |
| `data/reports/replay_validation/stage8_validation_readiness_report.json` | Generated review-only Stage 8 readiness report. Cross-checks the replay summary, Stage 6 realism gate, wallet replay scorecard, wallet outcome ledger, and candidate backfill targets. It can mark the Stage 8 validation loop complete while still reporting `0%` proof readiness when known outcomes, fillability, or score-ready market context are insufficient. Ignored by git as local generated research data. | replay validation completion gate, wallet-evidence collection queue | `utils/build_replay_validation_readiness.py`, `research.replay_validation_readiness` |
| `data/reports/historical_backfill/replayable_token_timelines_report.json` | Generated review-only Replayable Token Timelines closeout. Inventories blocked wallets, target mints, missing market-context rows, recovered price/liquidity rows, missing market cap/supply, and outcome-label gaps while keeping data readiness separate from layer completion. It never promotes, demotes, mutates wallet lists, trades, or treats unknown outcomes as rug/runner labels. Ignored by git as local generated research data. | replayable token timeline completion gate, pattern-analysis handoff | `utils/build_replayable_token_timelines.py`, `research.replayable_token_timelines`, `/api/replayable-token-timelines` |
| `data/desktop_api_session.json` | Local desktop API session token, process id, and start-time metadata for scoped metadata POST routes. Runtime secret; ignored by git. | native desktop shell, desktop API metadata POSTs | `desktop_api.py`, Tauri/fallback launcher |
| `data/candidate_ledger.json` | Operator decisions on candidates, ignored tokens, and protection adds. | dashboard Candidate Workbench | dashboard |
| `data/runtime_status.json` | Component heartbeat and freshness state, including `wallet_discovery` scheduler health. | dashboard Runtime Health, health report, native Ops/System | `core/runtime_status.py` callers |
| `data/process_locks/*.lock` | Launcher-only per-component start locks to avoid duplicate service spawns. | launcher | `launcher.py` |
| `data/bot_settings.json` | Strategy mode, thresholds, sizing, and confirmation settings. Optional Market Radar bounded holder probe: `market_radar_holder_check_enabled` (default false), `market_radar_max_holder_checks_per_cycle`, `market_radar_holder_check_timeout_seconds`. | dashboard Strategy Settings, scanner/scoring | dashboard settings form |
| `data/social_state.json` | Structured local social/sentiment events with account, platform, keywords, tickers, mints, sentiment, expiry, and raw/engagement fields. | dashboard Social Catalyst Tracker, scanner social matching, catalyst cards | `social/social_signal.py`, dashboard import form |
| `data/catalyst_cards.json` | Generated token thesis/outcome cards from snapshots, social matches, wallet confirmation, risk, and paper results. | dashboard Data Store Catalyst Cards tab, future Candidate Workbench | `core/catalyst_cards.py`, dashboard refresh |
| `data/position_action_intents.json` | Simulation-only prepared add-position and exit-early intents from Position Cockpit. This file must not execute live orders. | dashboard Position Cockpit | `core/position_cockpit.py`, dashboard buttons |
| `data/elite_wallets.json` | Wallet allow/promote list. | wallet intelligence, copy engine | manual edits/tools |
| `data/bad_wallets.json` | Wallet avoid/fade list. Current-compatible shape is a list of wallet address strings. Also blocks active re-entry during paper-watch sync. | wallet intelligence, copy engine, paper-watch sync | manual edits/tools, `utils/apply_wallet_review.py`, `utils/sync_paper_watch_wallets.py` |
| `data/tracked_wallets.json` | Wallets watched by the tracker. | wallet tracker/runtime | manual edits/tools |
| `dev_reputation.json` | Optional dev reputation data, including bonded/migrated token counts. | `core/dev_analyzer.py` | manual imports/future Axiom import |

### Rejection log semantics

Writers live in `analysis/rejection_hooks.py` (runtime, fail-open) and `analysis/export_sqlite_skips.py` (offline backfill).

| `source` (JSONL row) | Meaning |
| --- | --- |
| `scanner` | Strategy evaluated `should_trade=False`; the stored **rejection reason** is ranked from decision `reasons` plus optional payload `hard_block` / `hard_block_reason` (same helper as SQLite export). |
| `scanner_runtime` | Scanner path skipped before/with an explicit caller-supplied reason string (not re-ranked). |
| `market_radar` | Market Radar skip; reason is `skip_reason` from the summary or default `market_radar_skip`. |
| `market_radar_runtime` | Market Radar runtime precheck; caller-supplied reason; context includes `skip_bucket=runtime_precheck`. |
| `sqlite_skip_export` | Row produced by `python -m analysis.export_sqlite_skips` from `decision_records` payloads. |

**Ranked line (scanner + SQLite export):** `analysis/rejection_reason_pick.pick_ranked_rejection_reason` picks, in order: `hard_block_reason` when hard-blocked; else a non-generic `action_reason` (SQLite row column, when present); else the **first** scoring line in **document order** that matches an internal priority substring list (blocks, quote/sanity-style phrases, then trailing narrative such as `market_radar_skip:`); else the **last** scoring line; else a small default (`unknown_skip` / `sqlite_should_trade_false`). Re-backfilling SQLite can change older export rows relative to the previous “always last line” behavior.

Implementation pointers: `analysis/rejection_reason_pick.py`, tests in `tests/test_rejection_reason_pick.py` and `tests/test_rejection_hooks.py`.

## SQLite Store

SQLite file: `data/memetrader.db`

Managed by: `core/storage.py`

Security note: `.env` and SQLite database/WAL/SHM files should remain owner-only mode `600`; System Readiness reports unsafe private-file permissions.

| Table | Purpose | Notes |
| --- | --- | --- |
| `events` | Durable feed of raw wallet/signal events. | Unique index on time, event type, wallet, mint. |
| `alerts` | Durable alert and scoring records. | Stores score, edge verdict, should-trade, risk label, and raw payload JSON. |
| `decision_records` | Canonical candidate decision ledger. | Foundation started. Stores scanned candidate decision records: signal inputs, rule outcomes, risk/quote checks, final action, lane, and later paper result. UI and postmortem work should keep moving toward this table instead of reconstructing decisions from scattered JSON/files. |
| `trades` | Durable paper/live trade snapshots. | Stores summary columns plus full payload JSON. |
| `watchlist` | Durable protected-mint snapshots. | Mirrors manual watchlist entries. |
| `token_snapshots` | Durable token risk/performance snapshots. | Stores watchdog, scanner, and paper-trade lifecycle contexts, market data, risk label, full payload JSON, and candidate-feed display fields when captured. |
| `swap_ticks` | Durable trade-stream tick rows for Axiom-style candles. | Stores time, mint, signature, wallet, side, price, market cap, liquidity, token amount, SOL amount, source, and raw payload. Quote mint/amount and route program details are stored in the raw payload. Scanner writes `wallet_event_dex_route_delta` ticks when known DEX/Jupiter/Pump/Raydium/Meteora/Orca/OpenBook/Phoenix route programs are present and same-transaction SOL/USDC/USDT deltas allow execution-price math. It uses `wallet_event_dex_route_native_delta` when native SOL delta is usable, and `wallet_event_market_enriched` fallback ticks when route evidence is absent or market price is the only reliable price. Exact pool attribution remains future work. |

Desktop read-path indexes:

- `idx_token_snapshots_mint_time_desc` supports selected-token snapshot/candle reads ordered by latest mint/time.
- `idx_swap_ticks_mint_time_id_desc` supports selected-token swap-tick candle reads ordered by latest mint/time/id.

Backfill/sync utility: `utils/sync_state_to_sqlite.py`

Current stance: JSON files remain the practical runtime source for several panels, while SQLite is the durable query layer. Future work should consolidate more read paths onto SQLite once schemas are stable. Until then, desktop API payloads should declare their current source contract so mixed JSON/SQLite panels are visible to the operator.

## Current Desktop Source Contracts

These are current read contracts, not final architecture promises.

| Area | Current Canonical Read | Mirror / Secondary Source | Risk |
| --- | --- | --- | --- |
| Trades / Portfolio / paper replay | SQLite `trades` after JSON parity | `data/paper_trades.json` fallback | `/api/trades` and shared paper-trade state use SQLite only when bucket/key parity matches JSON. The local table was rebuilt from JSON and currently matches open 1, closed 10, failed 2. |
| Decisions / Replay Decision Ledger | SQLite `decision_records` | paper-trade fallback when no decisions exist | This is the intended canonical path for candidate decision review. |
| Selected-token detail / snapshots / candidates / candles fallback | JSON paper/manual position state plus SQLite `token_snapshots` | Social/catalyst/wallet JSON context | `/api/positions/{mint}` now declares position, snapshot, social, catalyst, and wallet-context sources. It warns when JSON position market fields are shown beside SQLite snapshot fields. |
| Alerts | SQLite `alerts` | `live_state.json` fallback | `/api/alerts` reads durable SQLite alert payloads first, falls back to live-state alerts if SQLite is empty, and warns when live-state retention differs from SQLite history. |
| Wallet stats | `data/wallet_performance.json`, `data/wallet_behavior.json`, wallet list JSON | SQLite `events` raw activity | Wallet payloads now declare their source contract, but wallet stats remain JSON-first until a dedicated schema/backfill exists. |
| Wallet quant report | `data/wallet_quant_report.json` | `data/wallet_performance.json`, `data/wallet_behavior.json`, `data/candidate_wallets.json`, `data/paper_watch_wallets.json`, SQLite `events/trades/token_snapshots` | Active wallet-evaluation artifact. It is still JSON-first until a dedicated wallet outcome table exists. |
| Wallet baseline comparison | `data/wallet_baseline_comparison.json` | `data/wallet_quant_report.json`, `data/wallet_outcome_ledger.json` | Review-only agreement/conflict layer. It shows where older wallet metrics are unsupported by unified outcome evidence. |
| Wallet candidate audit | `data/wallet_candidate_audit.json` | `data/wallet_outcome_ledger.json`, `data/wallet_baseline_comparison.json`, `data/wallet_review_decisions.json` replay metrics, `data/reports/wallet_backfills/wallet_stage4_review_report.json` | Human-review gate for generated promotion/demotion candidates. It is consumed by wallet-list apply as a stale-decision guard, exposed through `/api/wallet-candidate-audit`, exported to Obsidian candidate review notes, and can feed draft-only `/api/wallet-candidate-decision-prep`, review-only `/api/wallet-candidate-review-summary`, read-only `/api/wallet-candidate-collection-plan`, read-only `/api/wallet-candidate-collection-batch`, read-only `/api/wallet-candidate-blockers`, read-only `/api/wallet-candidate-context-recovery`, read-only `/api/wallet-candidate-context-recovery-runner`, and read-only `/api/wallet-candidate-context-recovery-closeout`, but still cannot apply changes without explicit approved decisions and list evidence. |
| Historical replay dataset | `data/historical_replay/replay_events.jsonl`, `data/historical_replay/summary.json` | unified records from `utils.build_wallet_outcome_ledger.build_records()` | Review-only dataset for past/future signal comparison. Events expose decision context, decision, fixed evaluation windows, execution assumptions, and later outcome separately. Window outcome counts summarize `30s`, `2m`, `5m`, and `15m` labels. Leakage count must stay zero before using a dataset for conclusions. |
| Wallet replay scorecard | `data/wallet_replay_scorecard.json` | `data/historical_replay/replay_events.jsonl` | Review-only behavioral scorecard. Separates known coverage from outcome performance and exposes repeated co-entry behavior. Conservative replay recommendations can be synced into review decisions, then must pass candidate-audit and wallet-list apply gates before list mutation. |
| Wallet cycle report | `data/wallet_cycle_report.json` | `data/tracked_wallets.json`, `data/paper_watch_wallets.json`, `data/bad_wallets.json`, `data/wallet_candidate_audit.json`, `data/wallet_replay_scorecard.json`, `data/wallet_review_decisions.json`, `data/wallet_list_update_audit.json` | One generated operator summary of wallet-list health, active observation counts, blocked/demoted rows, pending reviews, replay coverage, latest apply result, and attention flags. |
| Wallet candidate quality report | `data/wallet_candidate_quality_report.json` | `data/candidate_wallets.json`, `data/paper_watch_wallets.json`, `data/bad_wallets.json`, `data/wallet_behavior.json` | Review-only candidate triage report. Ranks active candidates and separately preserves bad-list/blocked evidence so candidate expansion can stay broad without silently re-admitting known weak wallets. |
| Wallet candidate quality review | `data/wallet_candidate_quality_review.json` | `data/wallet_candidate_quality_report.json`, `data/wallet_replay_scorecard.json`, `data/wallet_outcome_ledger.json` | Review-only bridge from broad candidate intake to human review. Strong candidate quality is not enough; replay and outcome evidence must also meet conservative gates before promotion-review-ready appears. |
| Stage 4 wallet review | `data/reports/wallet_backfills/wallet_stage4_review_report.json` | `data/reports/wallet_backfills/wallet_evidence_scorecard_report.json` | Review-only promotion/demotion gate. Consumes the Stage 3 scorecard and produces human-review actions without mutating wallet lists or trading. These rows now feed `data/wallet_candidate_audit.json` as review-only apply evidence. |
| Wallet candidate evidence plan | `data/wallet_candidate_evidence_plan.json` | `data/wallet_candidate_quality_review.json` | Review-only evidence collection queue. Converts observe-more/risk-review candidate rows into concrete missing evidence counts so the next data collection run can target the highest-quality wallets first. |
| Wallet candidate backfill targets | `data/wallet_candidate_backfill_targets.json` | `data/wallet_candidate_evidence_plan.json`, `data/historical_replay/replay_events.jsonl` | Review-only backfill action queue. Converts missing evidence into concrete work: collect wallet history for wallets with no local replay events, label unknown 15m outcomes for existing replay rows, collect more replay events, or resolve risk flags before further backfill. |
| Wallet history backfill | `data/wallet_backfills/wallet_history_backfill_report.json`, `data/wallet_evidence/wallet_history_evidence.jsonl`, `data/wallet_backfills/raw_transactions/*.jsonl` | `data/wallet_candidate_backfill_targets.json`, Solana read-only RPC, `data/historical_replay/replay_events.jsonl` | Review-only wallet evidence collector. Fetches bounded wallet signatures/transactions only when explicitly run with `--execute`, filters quote mints, extracts token buy/sell deltas, links known replay outcomes where available, records missing fields, and preserves raw transaction evidence. |
| Wallet evidence enrichment | `data/wallet_backfills/wallet_evidence_enrichment_report.json`, `data/wallet_evidence/wallet_history_evidence_enriched.jsonl` | `data/wallet_evidence/wallet_history_evidence.jsonl`, SQLite `token_snapshots`, SQLite `swap_ticks`, `data/historical_replay/replay_events.jsonl` | Review-only market/outcome enrichment. Prior snapshots can populate decision-time entry context; future snapshots and replay labels only populate later outcome fields. Does not promote wallets or alter trading logic. |
| Wallet missing market context | `data/wallet_backfills/wallet_missing_market_context_report.json` | `data/wallet_backfills/wallet_evidence_enrichment_report.json` | Review-only target queue. Identifies which token mints and time windows need future market snapshot/swap-tick backfill before wallet evidence can be trusted. Does not fetch data or alter wallet lists. |
| Wallet evidence readiness | `data/reports/wallet_backfills/wallet_evidence_readiness_report.json` | `data/wallet_candidate_backfill_targets.json`, `data/wallet_backfills/wallet_history_backfill_report.json`, `data/wallet_backfills/wallet_evidence_enrichment_report.json`, `data/wallet_backfills/wallet_missing_market_context_report.json`, `data/reports/historical_backfill/trusted_onchain_market_context_report.json`, `data/wallet_evidence/wallet_history_evidence.jsonl` | Review-only Stage 3 gate. Proves whether the wallet-history collection/evidence pipeline is current and idempotent, while keeping score readiness separate from collection completion. Does not promote wallets, trade, or treat incomplete evidence as edge. |
| Wallet evidence recommendations | `data/reports/wallet_backfills/wallet_evidence_recommendations_report.json` | `data/wallet_candidate_backfill_targets.json`, `data/wallet_backfills/wallet_history_backfill_report.json`, `data/reports/wallet_backfills/wallet_evidence_readiness_report.json` | Review-only wallet bucket report. Separates paper-watch candidates from observe-more, risk-review, and hold-no-edge wallets while keeping trusted promotion blocked by `do_not_promote_yet` until Stage 3 score readiness improves. |
| Wallet evidence lifecycle | `data/reports/wallet_backfills/wallet_evidence_lifecycle_report.json` | `data/wallet_evidence/wallet_history_evidence.jsonl` | Review-only behavior report. Groups wallet evidence by wallet/token to measure observed round trips, hold duration, buy-only rows, sell-only rows, action sequence, and accumulation/distribution behavior without price or PnL assumptions. |
| Wallet evidence scorecard | `data/reports/wallet_backfills/wallet_evidence_scorecard_report.json` | `data/reports/wallet_backfills/wallet_evidence_readiness_report.json`, `data/reports/wallet_backfills/wallet_evidence_recommendations_report.json`, `data/reports/wallet_backfills/wallet_evidence_lifecycle_report.json`, `data/wallet_backfills/wallet_evidence_enrichment_report.json` | Review-only integrated Stage 3 scorecard. Gives one row per candidate wallet with recommendation bucket, trust gate, lifecycle context, enrichment coverage, evidence blockers, and next action. This is the handoff input for Stage 4 review logic, not an auto-apply source. |
| Evidence Layer completion | `data/reports/wallet_backfills/evidence_layer_completion_report.json` | `data/reports/wallet_backfills/wallet_evidence_readiness_report.json`, `data/reports/wallet_backfills/wallet_evidence_scorecard_report.json`, `data/reports/wallet_reviews/wallet_candidate_context_recovery_closeout.json` | Review-only closeout for the Evidence Layer. It proves evidence capture/classification/routing is complete while keeping wallet score readiness, blocked wallets, and missing context explicit. This is a handoff to replayable token timelines, not promotion permission. |
| Market context recovery | `data/wallet_backfills/market_context_recovery_report.json`, SQLite `token_snapshots` | `data/wallet_evidence/wallet_history_evidence_enriched.jsonl`, archived wallet enrichment reports, `data/historical_replay/replay_events.jsonl`, `data/paper_trades.json`, `data/rejected_signals/rejections.jsonl`, `data/signal_contexts/contexts.jsonl` | Review-only migration recovery utility. Rehydrates SQLite snapshots from surviving decision-time-safe JSON context after local DB loss. Current/current-later data must not be used to fill old decision-time prices. |
| Historical market-context backfill | `data/reports/historical_backfill/historical_market_context_backfill_report.json`, `data/reports/historical_backfill/historical_market_context_backfill_records.jsonl` | `data/wallet_backfills/wallet_evidence_enrichment_report.json`, `data/wallet_backfills/raw_transactions/*.jsonl` | Review-only classifier for the post-migration market-context gap. It can recover partial quote-per-token context from preserved raw transaction balance deltas, but it does not assume lost SQLite `swap_ticks`, invent USD prices, create liquidity, substitute current prices, or mutate live trading logic. |
| Missing raw transaction recovery | `data/reports/historical_backfill/missing_raw_transaction_recovery_report.json`, `data/wallet_backfills/raw_transactions/missing_raw_transactions_*.jsonl` | `data/reports/historical_backfill/historical_market_context_backfill_report.json`, read-only Solana RPC | Review-only artifact recovery step. It fetches missing transaction signatures so the historical classifier can rerun with fuller raw evidence; it does not score wallets, promote/demote lists, or create market context by itself. |
| Historical quote-price enrichment | `data/reports/historical_backfill/historical_quote_price_enrichment_report.json`, `data/reports/historical_backfill/historical_quote_price_enrichment_records.jsonl`, `data/reports/historical_backfill/sol_usd_price_series.jsonl` | `data/reports/historical_backfill/historical_market_context_backfill_records.jsonl`, CoinGecko SOL market-chart range data when explicitly fetched | Review-only quote-price enrichment for WSOL-priced historical rows. Converts transaction-derived quote-per-token context into USD token price using a prior SOL/USD point only. It does not use future quote prices, does not create token liquidity/market cap, and does not make evidence score-ready by itself. |
| On-chain market-context recovery | `data/reports/historical_backfill/onchain_market_context_recovery_report.json`, `data/reports/historical_backfill/onchain_market_context_recovery_records.jsonl` | `data/reports/historical_backfill/historical_quote_price_enrichment_records.jsonl`, `data/wallet_backfills/raw_transactions/*.jsonl` | Review-only home-built reconstruction lane. It recovers liquidity candidates from raw transaction pool/vault token balances when local evidence is sufficient, preserves pre/post reserves, keeps later outcomes separate, and blocks market cap without decision-time supply. It does not mutate wallet lists, trade, or depend on paid provider snapshots. |
| On-chain supply evidence | `data/reports/historical_backfill/onchain_supply_evidence_report.json`, `data/reports/historical_backfill/onchain_supply_evidence_records.jsonl` | `data/reports/historical_backfill/onchain_market_context_recovery_records.jsonl`, `data/wallet_backfills/raw_transactions/*.jsonl` | Review-only supply classifier. It records token decimals from raw token balances, accepts only decision-time supply fields, and marks rows as needing archival mint supply when total supply is not locally provable. |
| Trusted historical snapshot gate | `data/reports/historical_backfill/trusted_historical_market_snapshot_report.json`, `data/reports/historical_backfill/trusted_historical_market_snapshot_records.jsonl` | `data/reports/historical_backfill/historical_market_context_backfill_records.jsonl` or `data/reports/historical_backfill/onchain_market_context_recovery_records.jsonl` | Review-only readiness gate for historical wallet evidence. It classifies which rows have enough decision-time price/liquidity/market-cap context to be used for wallet scoring, groups missing on-chain reconstruction requirements by token, and explicitly blocks incomplete rows. Provider snapshots are validation/fallback only. It does not fetch current prices, invent market context, mutate wallet lists, or touch execution. |
| Replay realism readiness | `data/reports/historical_backfill/replay_realism_readiness_report.json` | `data/historical_replay/summary.json`, `data/reports/historical_backfill/trusted_onchain_market_context_report.json`, `data/reports/historical_backfill/onchain_supply_evidence_report.json` | Review-only Stage 6 completion gate. It verifies live execution remains locked, replay has no decision-time leakage, fixed windows exist, fillability is classified, incomplete historical market context is blocked, and current-only supply is rejected. It separates Stage 6 contract completion from Stage 8 data score-readiness. |
| Replay validation readiness | `data/reports/replay_validation/stage8_validation_readiness_report.json` | `data/historical_replay/summary.json`, `data/reports/historical_backfill/replay_realism_readiness_report.json`, `data/wallet_replay_scorecard.json`, `data/wallet_outcome_ledger.json`, `data/wallet_candidate_backfill_targets.json` | Review-only Stage 8 completion gate. It proves the validation artifacts are aligned and stale-proof, while keeping proof readiness separate from contract completion. It does not promote wallets, trade, or treat incomplete replay coverage as edge. |
| Replayable token timelines | `data/reports/historical_backfill/replayable_token_timelines_report.json` | `data/reports/wallet_backfills/evidence_layer_completion_report.json`, `data/reports/wallet_reviews/wallet_candidate_context_recovery_closeout.json`, `data/wallet_backfills/wallet_missing_market_context_report.json`, `data/reports/historical_backfill/onchain_market_context_recovery_report.json`, `data/reports/historical_backfill/onchain_supply_evidence_report.json`, `data/reports/historical_backfill/replay_realism_readiness_report.json`, `data/reports/replay_validation/stage8_validation_readiness_report.json` | Review-only product-health closeout for token timelines. It proves the blocked wallet/mint timeline problem is fully inventoried and routed while keeping score readiness at zero until outcome labels, liquidity, market cap, and archival supply are real. |
| Signal contexts | `data/signal_contexts/contexts.jsonl`, `data/rejected_signals/rejections.jsonl` | SQLite `decision_records.payload_json`, scanner/Market Radar runtime payloads | V2 schema exists for rejected signals. Accepted paper entries still need matching context capture. |
| Unified signal outcome schema | `research.signal_schema` helper output; persistent ledger still pending | `core.signal_context`, rejection rows, paper trade rows, future replay rows | Produces comparable accepted/rejected records with wallet(s) -> signal context -> decision -> later outcome. Not yet a durable runtime ledger. |
| Wallet outcome ledger | `data/wallet_outcome_ledger.json` | `data/paper_trades.json`, `data/rejected_signals/rejections.jsonl`, `data/wallet_performance.json`, SQLite `token_snapshots`, `research.signal_schema` | First durable review artifact exists. It is generated, not live trading logic. Wallet-performance signals expand observation coverage; later snapshot outcomes are separated evaluation labels. |
| Replay visibility | `data/replay_visibility_report.json` | `data/rejected_signals/rejections.jsonl` | Review-only report, not proof of profitability. It helps inspect why signals were rejected and what context was available at the time. |
| Social / catalysts | `data/social_state.json`, `data/catalyst_cards.json` | social evidence frozen inside SQLite `decision_records` | Social payloads now declare the JSON source and decision-embedded evidence boundary. Historical decisions do not automatically change when social imports are edited later. |

Safe next work: wire holder/cluster risk into live candidate decisions. Wallet/social SQLite migration should wait for dedicated schemas and backfills; the source contracts now make their current JSON-first boundary explicit.

Known `token_snapshots.context` values:

| Context | Writer | Meaning |
| --- | --- | --- |
| `watchdog_check` | `core/rug_watchdog.py` | Protected-token check result with market, mechanics, holder, and prepared-exit status. |
| `scanner_skip` | `core/scanner.py` | Scanner evaluated a signal and rejected or skipped it before paper entry. |
| `scanner_entry_candidate` | `core/scanner.py` | Scanner evaluated a signal as trade-worthy before paper-entry prechecks/fill simulation. |
| `scanner_runtime_skip` | `core/scanner.py` | Scanner wanted to trade, but runtime prerequisites failed, such as no paper trader, missing market data, or invalid entry price. |
| `paper_entry_opened` | `paper_trader.py` | Paper trade opened successfully. |
| `paper_entry_failed` | `paper_trader.py` | Simulated paper buy fill failed. |
| `paper_partial_exit` | `paper_trader.py` | Paper trade took a partial exit. |
| `paper_exit_closed` | `paper_trader.py` | Paper trade closed. |
| JSON object with `context_type` and `recovery_version=market_context_recovery.v1` | `utils/recover_market_context_from_json.py` | Recovered decision-time market context restored from surviving JSON/JSONL artifacts after the migrated SQLite DB was lost. Review-only; not a live runtime writer. |

## Dashboard Panel Map

Dashboard file: `dashboard/dashboard.py`

| Panel | Backing Sources | Core Helpers |
| --- | --- | --- |
| Operator Brief | `live_state.json`, `data/paper_trades.json`, `data/manual_watchlist.json`, runtime state | `core/operator_brief.py` |
| Signal Pipeline | `live_state.json` alerts/signals | local dashboard aggregations |
| Command Center | process state, logs, candidate ledger | `core/process_guard.py`, `data/candidate_ledger.json` |
| Opportunity Inbox | `live_state.json` alerts | dashboard filters |
| Candidate Workbench | `live_state.json`, `data/candidate_ledger.json`, `data/manual_watchlist.json` | dashboard update helpers |
| Token Console | `live_state.json`, paper trades, manual watchlist, candidate ledger, `data/catalyst_cards.json` | `core/token_console.py`, dashboard catalyst-card helpers |
| Social Catalyst Tracker | `data/social_state.json`, `data/catalyst_cards.json` refresh | `social/social_signal.py`, `core/catalyst_cards.py` |
| Wallet Intelligence | `data/wallet_performance.json`, `data/wallet_behavior.json`, `data/candidate_wallets.json`, `data/paper_watch_wallets.json`, elite/bad wallet files | `core/wallet_labeler.py`, `core/wallet_lifecycle.py`, `core/wallet_behavior.py`, native Wallets tab |
| Paper Copy Engine | wallet performance and paper trade state | `core/wallet_copy_engine.py` |
| Performance Intelligence | `data/paper_trades.json`, `live_state.json` | `core/performance_analyzer.py` |
| Trade Postmortem | `data/paper_trades.json` | `core/trade_postmortem.py` |
| Replay Lab | `live_state.json`, `data/paper_trades.json` | `core/replay_analyzer.py` |
| Runtime Health | `data/runtime_status.json`, process table, logs | `core/runtime_status.py`, `core/process_guard.py` |
| System Readiness | env, files, imports, runtime status | `core/system_health.py` |
| Execution Safety | env/config/safety state | `core/execution_safety.py` |
| Data Store | `data/memetrader.db`, state files freshness, token snapshot context/source filters, `data/catalyst_cards.json` | `core/storage.py`, `core/data_freshness.py`, `core/catalyst_cards.py` |
| Strategy Settings | `data/bot_settings.json` | `core/settings_manager.py` |
| Manual Trade Protection | `data/manual_watchlist.json`, SQLite `events` for prepared exit intents | dashboard helpers, `core/rug_watchdog.py`, `core/protection_exit.py` |
| Position Cockpit | `data/paper_trades.json`, `data/manual_watchlist.json`, SQLite `token_snapshots`, `data/position_action_intents.json`, `data/runtime_status.json` | `core/position_cockpit.py`, dashboard helpers |
| Open/Closed/Failed Trades | `data/paper_trades.json` | dashboard trade rendering helpers |
| Alerts | `live_state.json` | dashboard rendering |
| Raw Debug Expanders | JSON state files | dashboard rendering |

## Desktop GUI Map

Desktop API file: `desktop_api.py`

Static GUI folder: `desktop_gui/`

Double-click launcher: `Start MemeTraderPro Desktop.command`

The desktop GUI is primarily a local presentation layer. It binds to `127.0.0.1`, serves static files, and exposes GET/HEAD read routes. The scoped mutation routes are protected-position amount metadata, protected-token watch entries, social research imports, wallet review decision metadata, and guarded wallet-list apply; token protection is required when the API is launched with a desktop session token. Buy, sell, auto-sell, and live execution routes remain unavailable.

| Route | Backing Sources | Notes |
| --- | --- | --- |
| `/` | `desktop_gui/index.html`, `desktop_gui/assets/*`, `desktop_gui/vendor/lightweight-charts.standalone.production.js` | Static Axiom-style cockpit shell with TradingView `lightweight-charts` rendering for selected-token charts. |
| `/api/health` | server process metadata | Reports read-only mode and live lock state. |
| `/api/overview` | `data/paper_trades.json`, `data/manual_watchlist.json`, `data/runtime_status.json`, social/catalyst/settings JSON, `data/wallet_performance.json`, `data/wallet_behavior.json` | Summary counts, runtime state, and cockpit wallet-confidence summary. |
| `/api/runtime` | `data/runtime_status.json` | Normalized runtime heartbeat summary plus raw runtime state. |
| `/api/readiness` | required state files, runtime status, read-only SQLite counts | Avoids live execution and reports degraded state as warnings/failures. |
| `/api/freshness` | `core.data_freshness.DataFreshness` | Source freshness report used for stale-state visibility. |
| `/api/positions` | `data/paper_trades.json`, `data/manual_watchlist.json` | Uses `core.position_cockpit.build_position_rows`. |
| `/api/positions/{mint}` | positions plus SQLite `token_snapshots`, `data/social_state.json`, `data/catalyst_cards.json`, `data/wallet_performance.json`, `data/wallet_behavior.json`, SQLite-first `/api/trades` paper outcomes (falls back to `data/paper_trades.json` where needed) | Selected-position detail payload with snapshot trend metrics, read-only protection summary, local signal/catalyst matches, and wallet confidence context. Payload declares `position_source`, `snapshot_source`, social/catalyst/wallet context contracts, and `mixed_market_fields` when JSON position market fields appear beside SQLite snapshots. Wallet context uses the canonical SQLite-first trade read when explicit paper state is not supplied. |
| `/api/candidates` | SQLite `token_snapshots` scanner contexts | Read-only live launch feed. Dedupes recent scanner candidates by mint and exposes image URL, name/symbol, market cap, liquidity, tx count, holder count, wallet score, risk, and pass/skip/block reasons when available. |
| `/api/decisions` | SQLite `decision_records`, linked SQLite `trades` fallback for missing result fields | Read-only canonical decision feed. Exposes recent candidate decisions, action, lane, score, risk, quote pass/fail flags, paper outcome fields, and compact payload/result JSON. If an older decision lacks `result_json`, the API can enrich the response from a linked trade payload carrying `signal_metadata.decision_id`. Supports filters such as `filter=quote_failed` and `lane=main`. Feeds the static desktop Replay tab and the React/Tauri Replay Decision Ledger. |
| `/api/candidate-wallets` | `data/candidate_wallets.json` | Watch-only discovered wallets for manual review. Read-only; does not promote wallets or execute trades. |
| `/api/wallet-lifecycle` | `data/tracked_wallets.json`, `data/paper_watch_wallets.json`, `data/candidate_wallets.json`, `data/wallet_performance.json`, `data/wallet_behavior.json` | Read-only promotion/demotion queue with trade count, win/loss record, win rate, total PnL, average PnL, score, behavior labels, rolling windows, postmortem summary, and lifecycle recommendation. |
| `/api/wallet-quant` | `data/tracked_wallets.json`, `data/paper_watch_wallets.json`, `data/wallet_performance.json`, `data/wallet_behavior.json`; generated report target `data/wallet_quant_report.json` | Read-only Quant Wallet Tracker review payload. Groups wallets by trusted/paper-watch/candidate evidence and exposes recommendation counts for promotion review, demotion review, and hold-more-data. |
| `/api/wallet-replay-review` | `data/wallet_replay_scorecard.json` | Read-only replay scorecard triage endpoint. Separates reviewable wallets from low-coverage wallets using known/fillable replay evidence, surfaces repeated co-entry pairs for ecosystem review, and includes conservative machine recommendations plus a markdown operator report for human review/export. The Obsidian exporter also renders this as `MemeTraderPro/Dashboards/Wallet Replay Ecosystem Review.md`. |
| `/api/wallet-cycle` | `data/wallet_cycle_report.json`; computed fallback from tracked wallets, paper-watch rows, bad wallets, candidate audit, replay scorecard, review decisions, and wallet-list apply audit | Read-only wallet feedback-loop health endpoint. Summarizes active/blocked paper-watch counts, pending promotion/demotion reviews, bad-wallet blocking, replay coverage, latest apply outcome, and attention flags for operator review. The Obsidian exporter also renders this as `MemeTraderPro/Dashboards/Wallet Cycle Report.md`. |
| `/api/wallet-candidate-quality` | `data/wallet_candidate_quality_report.json`; computed fallback from candidate wallets, paper-watch rows, bad wallets, and wallet behavior | Read-only candidate-quality endpoint. Shows active ranked candidates, blocked/bad-listed candidates, evidence reasons, risk flags, and recommended observation bucket. Does not promote, demote, or execute trades. The Obsidian exporter renders this as `MemeTraderPro/Dashboards/Wallet Candidate Quality.md`. |
| `/api/wallet-candidate-quality-review` | `data/wallet_candidate_quality_review.json`; computed fallback from candidate quality, replay scorecard, and wallet outcome ledger | Read-only candidate-quality review endpoint. Cross-checks top ranked candidate wallets against replay/outcome evidence before any human-review suggestion. Does not approve or apply wallet changes. The Obsidian exporter renders this as `MemeTraderPro/Dashboards/Wallet Candidate Quality Review.md`. |
| `/api/wallet-candidate-evidence-plan` | `data/wallet_candidate_evidence_plan.json`; computed fallback from candidate quality review | Read-only evidence collection endpoint. Shows missing replay-known, fillable, and outcome counts per top candidate wallet and recommends the next collection action. Does not approve, apply, or trade. The Obsidian exporter renders this as `MemeTraderPro/Dashboards/Wallet Candidate Evidence Plan.md`. |
| `/api/wallet-candidate-backfill-targets` | `data/wallet_candidate_backfill_targets.json`; computed fallback from candidate evidence plan | Read-only backfill target endpoint. Shows which candidate wallets need wallet-history collection, outcome-label backfill, additional replay events, or risk review. Does not approve, apply, or trade. The Obsidian exporter renders this as `MemeTraderPro/Dashboards/Wallet Candidate Backfill Targets.md`. |
| `/api/wallet-history-backfill` | `data/wallet_backfills/wallet_history_backfill_report.json`; computed dry-run fallback from candidate backfill targets | Read-only wallet-history backfill endpoint. Shows processed wallets, target status, evidence counts, block reasons, and preliminary wallet metrics. Does not fetch data, approve, apply, or trade from the API route. The Obsidian exporter renders this as `MemeTraderPro/Dashboards/Wallet History Backfill.md`. |
| `/api/wallet-evidence-enrichment` | `data/wallet_backfills/wallet_evidence_enrichment_report.json`; empty computed fallback when no report exists | Read-only wallet-evidence enrichment endpoint. Shows enriched evidence rows, entry-context coverage, outcome-label coverage, and missing-context counts. Does not fetch data, approve, apply, or trade from the API route. The Obsidian exporter renders this as `MemeTraderPro/Dashboards/Wallet Evidence Enrichment.md`. |
| `/api/wallet-missing-market-context` | `data/wallet_backfills/wallet_missing_market_context_report.json`; computed fallback from current enrichment report | Read-only missing market-context endpoint. Shows target mints, affected wallets, deduped missing-context evidence row counts, and proposed backfill windows. Does not fetch data, approve, apply, or trade from the API route. The Obsidian exporter renders this as `MemeTraderPro/Dashboards/Wallet Missing Market Context.md`. |
| `POST /api/wallet-review-decision` | `data/wallet_review_decisions.json` | Scoped local metadata update for operator wallet review decisions: approve promotion, approve demotion, hold, or reject. Does not apply changes to tracked/bad wallet lists and does not execute trades. |
| `/api/wallet-review-apply` | `data/wallet_review_decisions.json`, `data/tracked_wallets.json`, `data/paper_watch_wallets.json`, `data/bad_wallets.json`, `data/wallet_list_update_audit.json` | GET returns dry-run preview without raw wallet lists. POST requires exact confirmation `APPLY_WALLET_REVIEW`, runs the controlled apply tool, creates backups/audit records, and still does not execute trades. |
| `/api/events` | SQLite `events` | Read-only scanner tape for raw tracked-wallet buy/sell events before strategy filters promote them into launch candidates. Exposes mint, wallet, age, amount, and wallet score fields when present in payload JSON. |
| `/api/candles`, `/api/tokens/{mint}/candles` | SQLite `swap_ticks`, fallback SQLite `token_snapshots` | Uses `core.position_cockpit.build_candles`; supports strict read-only `metric=market_cap`, `metric=price`, or `metric=liquidity` plus `interval=1`, `5`, `30`, or `60`. Prefers tick-derived candles when `swap_ticks` exist for the mint and reports `sample_kind=swap_tick`, `tick_count`, and `trade_stream_active=true`. Falls back to sampled quote/snapshot candles with `sample_kind=sampled_quote` when tick rows are absent. |
| `/api/tokens/{mint}/snapshots` | SQLite `token_snapshots` | Oldest-to-newest token snapshot rows. Payload declares `source=sqlite_token_snapshots` and `source_detail=data/memetrader.db:token_snapshots`. |
| `/api/trades` | SQLite `trades`, fallback `data/paper_trades.json` | Open/closed/failed trade state. Canonical path prefers durable SQLite trades and falls back to JSON when SQLite has no rows. Feeds the desktop Portfolio view, static desktop Portfolio panel, paper replay, and selected-token trade lifecycle. Payload declares `source=sqlite_trades` when parity passes, plus JSON fallback diagnostics. |
| `/api/paper-review` | Shared paper-trade state, `data/wallet_behavior.json` | Read-only paper profitability review with readiness gaps, paper metrics, main-vs-exploration lane metrics, Decision Ledger lane report, **`sample_progress` per lane** and **`decision_lineage` coverage** (`decision_id`), entry/exit/failure reasons, and wallet-label exposure. Shared paper-trade state is SQLite-first after JSON parity. |
| `/api/winner-patterns` | Shared paper-trade state | Read-only winner-pattern review comparing closed winners against closed losers. Used for paper tuning only; live execution remains locked. Shared paper-trade state is SQLite-first after JSON parity. |
| `/api/watchlist` | `data/manual_watchlist.json` | Protected manual positions. |
| `POST /api/watchlist/protected-token` | `data/manual_watchlist.json` | Scoped local metadata update for adding/updating a manual protected token from the desktop Protection tab. Watch/alert only; live execution and auto-sell remain locked. |
| `POST /api/watchlist/protected-amount` | `data/manual_watchlist.json` | Scoped local metadata update for protected-position amount/decimals/raw/test flag only. Live execution and auto-sell remain locked. |
| `/api/alerts` | SQLite `alerts`, fallback `live_state.json` alerts | Prefer durable SQLite alerts; optional `limit` query. Root live-state alerts only when SQLite is empty. Payload declares source, mirror diagnostics when counts differ. |
| `/api/social` | `data/social_state.json` | Local social events/signals; supports both `events` and legacy `signals` keys. Payload declares the social JSON source, catalyst-card JSON source, and the separate decision-embedded social evidence boundary. |
| `POST /api/social/import` | `data/social_state.json` | Scoped local metadata update for importing a tweet/social signal from the desktop Signals/Pulse panel. Local research only; does not trigger trades. |
| `/api/catalyst-cards` | `data/catalyst_cards.json` | Generated catalyst cards. |
| `/api/settings` | `data/bot_settings.json` | Read-only settings snapshot. |

## Decision Path Map

| Decision | Inputs | Modules |
| --- | --- | --- |
| Candidate decision ledger | mint, detected time, source event, signal wallets, social matches, risk/holder/mechanics checks, quote/liquidity checks, rule outcomes, final action, lane, later result | `core/decision_ledger.py`, `core/scanner.py`, `paper_trader.py`, `core/storage.py`, `desktop_api.py` |
| Candidate scoring | wallet signal, market/liquidity, token age, launch age, dev reputation, token mechanics | `core/scanner.py`, `core/scoring_engine.py`, `core/confirmation_filter.py` |
| Anti-rug decision | liquidity, token age, dev score, token mechanics, sell quote feasibility | `core/anti_rug.py`, `core/dev_analyzer.py`, `core/token_inspector.py` |
| Token-2022 risk | mint account owner, parsed extensions, authority fields, default account state | `core/token_inspector.py` |
| Dev reputation | developer wallet metadata, bonded/migrated token count, known bad/suspicious flags | `core/dev_analyzer.py`, `dev_reputation.json` |
| Paper entry | candidate score, risk result, quote result, settings, position sizing, paper lane, explicit non-live eligibility for exploration | `paper_trader.py`, `core/scanner.py`, `core/paper_exploration.py` |
| Paper Exploration Lane | near-miss score, edge score, confirmation result, strategy guard, hard-block status, market sanity, buy quote, sell quote, configured exploration size | `core/paper_exploration.py`, `core/scanner.py`, `data/paper_trades.json`, `/api/paper-review` |
| Decision lane report | main/exploration/protected-manual candidate decisions, opens, skips, quote failures, hard blocks, social/wallet confirmations, and recorded outcomes | SQLite `decision_records`, `desktop_api.py`, native Paper Review |
| Paper exit | trade state, partial profit rules, stops, exit advisor | `paper_trader.py`, `core/exit_advisor.py` |
| Protected mint status | current price/liquidity, peak/baseline drawdown, token mechanics, wallet token balance, prepared simulation exit intent | `core/rug_watchdog.py`, `core/protection_exit.py`, `core/token_balance.py` |
| Fast open-position monitor | open paper/protected positions, lightweight market snapshots, liquidity/market-cap/price drawdown, quote degradation, exit-rule state | planned boundary; should be separate from `core/rug_watchdog.py` deep inspection |
| Holder concentration risk | largest token accounts, supplied holder rows/account balances; bounded scanner checks for quote-worthy candidates | `core/scanner.py`, `core/rug_watchdog.py`, `core/holder_concentration.py` |
| Token snapshots | watchdog status, scanner entries/skips, paper entries/exits, market metrics, mechanics, holder concentration, prepared exit quote status | SQLite `token_snapshots`, `core/rug_watchdog.py`, `core/scanner.py`, `paper_trader.py` |
| Trade-stream candles | parsed swap/tick rows grouped into 1s/5s/30s/1m OHLC candles; sampled snapshots only as fallback | SQLite `swap_ticks`, `desktop_api.py`, `core.position_cockpit.build_candles`, `core.scanner.Scanner.record_swap_tick_from_event`, future exact DEX instruction parser |
| Live launch feed | recent `scanner_skip`, `scanner_entry_candidate`, and `scanner_runtime_skip` snapshots, including Dexscreener metadata when present | `desktop_api.py`, SQLite `token_snapshots`, `infra/market_checker.py`, native desktop GUI |
| Scanner tape | raw tracked-wallet buy/sell events before candidate qualification | `desktop_api.py`, SQLite `events`, native desktop GUI |
| Candidate wallet discovery | local scanner buy/sell events, winning paper-trade mints, optional read-only Solana token-owner deltas, existing wallet performance, bad-wallet block list filtering, blocked-candidate audit rows | `core/wallet_discovery.py`, `utils/discover_candidate_wallets.py`, `core/wallet_discovery_scheduler.py`, `data/candidate_wallets.json`, `data/wallet_discovery_status.json` |
| Wallet discovery scheduler | timed local-only discovery cycle by default, paper-watch sync, wallet apply dry-run preview, scheduler heartbeat | `core/wallet_discovery_scheduler.py`, `utils/run_wallet_discovery_scheduler.py`, `launcher.py`, `data/runtime_status.json` |
| Wallet lifecycle | candidate review action, paper-watch membership, tracked-wallet paper performance, win rate, average PnL, score, rolling 7d/30d windows, behavior labels, per-wallet postmortem rollups | `core/wallet_lifecycle.py`, `utils/sync_paper_watch_wallets.py`, `data/paper_watch_wallets.json`, `data/wallet_behavior.json`, native Wallets tab |
| Wallet behavior and postmortem | paper trade outcomes, signal token ages, hold times, signal volume, paper win/loss windows, best/worst wallet outcome, exit/failure reasons | `core/wallet_behavior.py`, `core/wallet_discovery_scheduler.py`, `data/wallet_behavior.json` |
| Wallet Quant V2 profile | wallet ROI, win rate, average hold duration, rug association, entry timing quality, average PnL multiple, runner/rug participation, preferred token age, preferred liquidity range, conviction sizing, relationships, coordinated entries, data completeness | `wallets/wallet_metrics.py`, `wallets/wallet_relationships.py`, `wallets/wallet_profiles.py`, `wallets/wallet_score.py`, `core/wallet_quant.py`, `data/wallet_quant_report.json` |
| Signal context capture | triggering wallets, wallet quality, cluster timing, liquidity, market cap, price, volume, token age, holder concentration, slippage/delay assumptions, score reasons, market-regime tags | `core/signal_context.py`, `analysis/signal_context_logger.py`, `analysis/rejection_hooks.py`, `analysis/export_sqlite_skips.py`, `data/signal_contexts/contexts.jsonl` |
| Unified signal outcome record | accepted/rejected/failed record type, wallets, signal context, trade/skip decision, labeled later token outcome, replay assumptions, research safety flags | `research/signal_schema.py`, `research/outcome_labeler.py`, future wallet-outcome ledger |
| Wallet outcome ledger | per-wallet total/accepted/rejected/observed signals, known outcomes, runner/rug/dead participation, average PnL, average liquidity, average token age, cluster duration, market-regime breakdown, confidence, promotion/demotion scores, review-only recommendation | `wallets/wallet_outcome_ledger.py`, `wallets/wallet_promotion_engine.py`, `research/outcome_linker.py`, `utils/build_wallet_outcome_ledger.py`, `data/wallet_outcome_ledger.json` |
| Wallet baseline comparison | per-wallet comparison of quant-report recommendation versus outcome-ledger recommendation, including agreement/conflict/unconfirmed/missing-evidence status | `wallets/wallet_baseline_comparison.py`, `utils/build_wallet_baseline_comparison.py`, `data/wallet_baseline_comparison.json` |
| Wallet candidate audit | per-candidate review packet with apply blocked, evidence gates, known/fillable outcome sample, source coverage, runner/rug/dead counts, average PnL, replay rates, or Stage 4 review metrics, confidence, recommendation reasons, and resolved applied rows | `wallets/wallet_candidate_audit.py`, `utils/build_wallet_candidate_audit.py`, `data/wallet_candidate_audit.json` |
| Historical replay dataset | accepted/rejected/failed records converted into decision-time-safe replay events with fixed windows, execution assumptions, fill-status classification, per-window outcome counts, and separated later outcome labels | `research/historical_replay_dataset.py`, `utils/build_historical_replay_dataset.py`, `data/historical_replay/replay_events.jsonl` |
| Wallet replay scorecard | per-wallet replay coverage, known/fillable outcomes, regime exposure, co-entry partners, repeated co-entry pairs, read-only review queues for reviewable versus low-coverage wallets, conservative promotion/demotion/hold recommendations, markdown/Obsidian operator report rendering, and local review-decision sync | `wallets/wallet_replay_scorecard.py`, `wallets/wallet_replay_review.py`, `utils/build_wallet_replay_scorecard.py`, `utils/sync_replay_wallet_decisions.py`, `obsidian_export/intelligence_notes.py`, `data/wallet_replay_scorecard.json`, `/api/wallet-replay-review` |
| Wallet cycle report | tracked count, active paper-watch count, blocked paper-watch count, bad-wallet count, pending promotion/demotion counts, approved decision counts, replay scorecard counts, latest apply summary, and attention flags | `wallets/wallet_cycle_report.py`, `utils/build_wallet_cycle_report.py`, `data/wallet_cycle_report.json`, `/api/wallet-cycle`, `obsidian_export/intelligence_notes.py` |
| Replay visibility | no-trade context inspection, rejection reason, triggering wallets, market state, risk state, execution assumptions, counterfactual placeholder, replay notes | `core/replay_visibility.py`, `utils/build_replay_visibility_report.py`, `data/replay_visibility_report.json` |
| Selected-token wallet confidence | token-matched wallet signals, paper trade wallet attribution, wallet performance, behavior labels, and postmortem rollups | `desktop_api.py`, native Details tab, `data/wallet_performance.json`, `data/wallet_behavior.json`, SQLite-first `/api/trades` paper outcomes |
| Cockpit wallet confidence | recent wallet signals, open paper-trade wallet attribution, wallet performance, behavior labels, and postmortem rollups | `desktop_api.py`, native Cockpit tab, `data/wallet_performance.json`, `data/wallet_behavior.json`, `data/paper_trades.json` |
| Wallet review decision | operator approve/hold/reject selection for a lifecycle row | `desktop_api.py`, native Wallets tab, `data/wallet_review_decisions.json` |
| Wallet list apply | approved review decisions, lifecycle report, tracked wallets, paper-watch wallets, bad wallets, and candidate audit; promotions require paper-watch evidence plus lifecycle or candidate-audit support; demotions can remove tracked wallets or mark paper-watch wallets as `demote_review`; apply runs under a local lock with unique backup stamps | `core/wallet_list_apply.py`, `utils/apply_wallet_review.py`, `desktop_api.py`, native Wallets tab, `data/wallet_review_decisions.json`, `data/wallet_candidate_audit.json`, `data/wallet_list_update_audit.json` |
| Catalyst cards | token snapshots, social matches, wallet confirmation, risk fields, paper outcomes | `core/catalyst_cards.py`, `data/catalyst_cards.json` |
| Social import | pasted account, text, URL, keywords, and optional mint from desktop Signals/Pulse | `desktop_api.py`, `data/social_state.json`, native/static desktop GUI |
| Social event matching | token name/symbol/mint against active social keywords, tickers, and extracted mints | `social/social_signal.py`, `core/scanner.py` |
| Automated social collectors | Reddit/X/Telegram/Discord source posts normalized into `data/social_state.json`, then attached to catalyst cards and decision records | `social/reddit_collector.py`, `social/social_signal.py`, `data/runtime_status.json`; X/Telegram/Discord future |
| Social-to-price alignment | catalyst event timestamp, selected mint, event-time market/liquidity, 5m/15m/1h/4h follow-up, outlier flags | planned; should read market snapshots from SQLite `token_snapshots` / `swap_ticks` and write back to catalyst/decision evidence |
| Position cockpit action intent | selected paper/protected token, operator button press, live execution lock | `core/position_cockpit.py`, `data/position_action_intents.json` |
| Live execution permission | environment, safety flags, wallet/config, explicit arming | `core/execution_safety.py`, `execution/*` |

## Freshness And Reliability Notes

- `data/runtime_status.json` is the heartbeat file. If a component is stale there, the dashboard should treat related data as stale too.
- `core/data_freshness.py` classifies important state sources as fresh, stale, old, missing, or broken and renders those results in the Data Store panel.
- `live_state.json` and `data/live_state.json` can diverge. Future work should pick a canonical live-state file and make the other a compatibility alias or remove it.
- SQLite is useful for querying and persistence, but several panels still read JSON directly. Treat SQLite as durable memory, not yet the only source of truth. `/api/alerts` prefers SQLite alert rows with a live-state fallback, and `/api/trades` prefers SQLite trade rows with a paper-trades JSON fallback (`load_paper_trade_state` parity).
- Candidate decisions should read from SQLite `decision_records` as the canonical source (desktop `/api/decisions` enriches missing `result_json` from linked SQLite trades where available). Scanner records include holder concentration evidence for quote-worthy candidates; true linked-wallet graph risk remains explicitly not checked until a real owner/funder graph source exists.
- Manual social import is only the bridge for operator-entered evidence. Reddit collection writes normalized social `events` and collector freshness, but it is standalone/unscheduled until observed safely. Future collectors must use the same social evidence path instead of creating a separate social source of truth.
- Deep watchdog state and fast open-position monitoring should be separate sources. The UI must not label slow deep-inspection freshness as real-time exit protection.
- Any future live execution feature must write an audit record before and after every attempted order.

## When Adding A New Feature

1. Decide the runtime source file/table.
2. Decide the durable history table or JSON ledger.
3. Add a freshness/last-updated field.
4. Add the dashboard panel or Token Console field.
5. Update this map and `research/BUILD_PLAN.md`.
