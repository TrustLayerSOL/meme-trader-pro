/**
 * Live execution intentionally disabled until validation and approval gates are complete.
 */
export class LiveTrader {
  async run() {
    return { status: "disabled", reason: "live execution intentionally disabled in v3 scaffold" };
  }
}
